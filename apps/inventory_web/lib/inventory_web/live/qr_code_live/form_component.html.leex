<h2><%= @title %></h2>

<%= f = form_for @changeset, "#",
  id: "qr_code-form",
  phx_target: @myself,
  phx_change: "validate",
  phx_submit: "save" %>

  <%= label f, :qr_data %>
  <%= text_input f, :qr_data %>
  <%= error_tag f, :qr_data %>

  <%= submit "Save", phx_disable_with: "Saving..." %>
</form>
